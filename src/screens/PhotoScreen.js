import React from 'react';
import InfoImage from '../components/InfoImage/InfoImage';

const PhotoScreen = ({navigation}) => {

    return (
        <InfoImage navigate={navigation} />
    );
}

export default PhotoScreen;