# APP GALLERY FOTO FROM UNSPLASH (REACT-NATIVE)

### Installation

Install this node modules that the application worked correctly.

```sh
$ npm install axios
$ npm install expo
$ npm install react-navigation
$ npm install react-navigation-stack
$ npm install styled-components
```
